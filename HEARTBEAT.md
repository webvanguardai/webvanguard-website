# HEARTBEAT.md

# CEO Mode: Constant motion for WebVanguard

CEO heartbeat triggers: every ~30 minutes during waking hours (8am-10pm Dubai). On each trigger, run the following internal checks via subagents:

- Check website metrics (if API connected) or latest snapshot
- Scan industry news/trends (Brave Search fallback)
- Identify 1 quick win or optimization
- Review inbox/calendar for urgent client actions

If nothing critical, reply HEARTBEAT_OK.
If something actionable, report findings and proposed next steps.

# Keep this file empty (or with only comments) to skip heartbeat API calls.

# Add tasks below when you want the agent to check something periodically.
